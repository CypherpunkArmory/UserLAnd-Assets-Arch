version: '3.2'


services:
   basebuild:
     privileged: true
     build: 
       context: .
       dockerfile: Dockerfile.build_image
       args:
         RUN_CMD: input/build_image.sh ${IMAGE_ARCH}
     volumes:
       - ./output:/output
       - ./input:/input
