version: '3.2'

services:
   imagebuild:
     build: 
       args:
         IMAGE_DISTRO: lfdevs/archlinuxarm
         IMAGE_PLATFORM: linux/arm64/v8
         IMAGE_VERSION: base-devel
         IMAGE_ARCH: arm64v8
         QEMU_FILE: qemu-aarch64-static
